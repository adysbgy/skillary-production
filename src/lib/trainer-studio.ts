import { z } from "zod";
export const trainerIntakeSchema=z.object({name:z.string().trim().min(2).max(120),email:z.string().email(),linkedinUrl:z.string().url().startsWith("https://www.linkedin.com/"),websiteUrl:z.union([z.literal(""),z.string().url()]).optional(),headline:z.string().max(180).optional(),expertise:z.array(z.string().max(80)).max(12).default([])});
export function trainerSlug(name:string){return name.toLowerCase().normalize("NFKD").replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"").slice(0,80)}
export function publishReadiness(t:{name:string;headline:string;shortBio:string;portraitUrl:string|null;photoRights:string|null;expertise:string[];experience:unknown;consentedAt:Date|null;verification:string;evidence:unknown}){const missing:string[]=[];if(!t.name)missing.push("name");if(!t.headline)missing.push("headline");if(!t.shortBio)missing.push("short bio");if(!t.portraitUrl)missing.push("approved portrait");if(!t.photoRights)missing.push("photo rights");if(!t.expertise.length)missing.push("expertise");if(!Array.isArray(t.experience)||!t.experience.length)missing.push("experience");if(!t.consentedAt)missing.push("trainer consent");if(t.verification==="VERIFIED"&&(!Array.isArray(t.evidence)||!t.evidence.length))missing.push("verified evidence");return missing}
