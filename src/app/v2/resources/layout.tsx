import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Resource Pelatihan Gratis — Skillary",
  description: "Unduh template, e-book, modul, dan checklist pelatihan gratis untuk HR & L&D.",
};

export default function ResourcesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
