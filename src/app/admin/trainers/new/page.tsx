import {auth} from "@/lib/auth";import {redirect} from "next/navigation";import Link from "next/link";import {TrainerIntakeForm} from "@/components/trainers/TrainerIntakeForm";
export default async function NewTrainer(){const s=await auth();if((s?.user as {role?:string})?.role!=="ADMIN")redirect("/dashboard");return <div className="mx-auto max-w-5xl"><Link href="/admin/trainers" className="text-sm font-bold text-orange-700">← Trainer Studio</Link><h1 className="mt-5 text-3xl font-extrabold">Quick trainer intake</h1><p className="mb-8 mt-2 max-w-2xl text-sm leading-6 text-black/55">Masukkan identity seed. Setelah draft dibuat, lakukan public research dan minta trainer menyetujui data serta foto sebelum publish.</p><TrainerIntakeForm/></div>}
